extends ../layout

block content
  h2= `Successfully added "${book.title}"`
  p= book.body
  a(href="../") &larr; Back
  | | 
  a(href="/books/" + book.id + "/edit") Edit Book 
  | | 
  a(href="/books/" + book.id + "/delete") Delete Book